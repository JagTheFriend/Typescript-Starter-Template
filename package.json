{
  "name": "minimal-nodejs-typescript-starter-template",
  "version": "1.0.0",
  "description": "Minimalist Node JS + Typescript starter template",
  "main": "dist/main.js",
  "scripts": {
    "prestart": "npm run build",
    "build": "rimraf dist && tsc",
    "start": "node dist/main.js",
    "dev": "tsx watch ./src/main.ts",
    "lint": "biome lint ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JagTheFriend",
  "license": "CC0-1.0",
  "dependencies": {
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "tsx": "^4.10.5",
    "@biomejs/biome": "^1.7.3",
    "@types/node": "^17.0.25",
    "prettier": "^2.6.2",
    "rimraf": "^3.0.2"
  }
}
